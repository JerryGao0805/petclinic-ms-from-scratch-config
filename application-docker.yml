management:
  endpoints:
    web:
      exposure:
        include: health,refresh,info
  endpoint:
    health:
      probes:
        enabled: true
  info:
    env:
      enabled: true
    git:
      mode: simple

info:
  config:
    version: ${config.client.version:unknown}