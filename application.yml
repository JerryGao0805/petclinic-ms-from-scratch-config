demo:
  message: hello-from-config-server
  note: "config-repo-smoke-2026-03-07-01"

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
